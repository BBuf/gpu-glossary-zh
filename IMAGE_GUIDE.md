# 图片使用指南

本项目支持在 Markdown 文件中使用外链图片，自动渲染为美观的卡片式容器。

## 📸 支持的图片格式

### 1. 基本图片（无说明）

```markdown
![可选的 alt 文字](https://example.com/image.png)
```

渲染效果：
- 图片居中显示
- 带阴影和圆角的卡片容器
- 如果有 alt 文字，会显示在图片下方

### 2. 图片 + 说明（推荐）

```markdown
![](https://example.com/image.png)

> 这是图片的详细说明文字，支持**加粗**、*斜体*和[链接](https://example.com)。
```

渲染效果：
- 图片和说明在同一个容器内
- 说明文字以斜体显示
- 支持 Markdown 格式（链接、加粗、斜体等）

## 🎨 样式特点

生成的图片容器具有以下特点：

- **暗色主题**: 背景色 `#161b22`，边框色 `#30363d`
- **响应式**: 自动适应不同屏幕尺寸
- **懒加载**: 使用 `loading="lazy"` 提升性能
- **圆角阴影**: 现代化的视觉效果
- **卡片式**: 统一的容器样式

## 💡 使用建议

### ✅ 推荐做法

1. **使用图床外链**
   ```markdown
   ![](https://files.mdnice.com/user/59/xxx.png)
   
   > 图片说明：这是一个示例图片。
   ```

2. **添加有意义的说明**
   - 说明图片内容
   - 注明图片来源
   - 提供相关链接

3. **图片说明格式**
   ```markdown
   > 图片标题或主要描述。详细说明。改编自 [来源链接](https://example.com)。
   ```

### ❌ 避免的做法

1. **不要使用本地图片路径**
   ```markdown
   <!-- ❌ 不推荐 -->
   ![](../images/local-image.png)
   ```

2. **不要使用没有协议的链接**
   ```markdown
   <!-- ❌ 不推荐 -->
   ![](//example.com/image.png)
   ```

3. **避免过大的图片**
   - 图片宽度建议不超过 1200px
   - 文件大小建议不超过 500KB
   - 使用压缩工具优化图片

## 📋 实际示例

### 示例 1：带说明的技术图表

```markdown
![](https://files.mdnice.com/user/59/74675656-a0d7-4c7a-a354-67b33aa2e9b7.png)

> 固定管线设备架构示意图 (G71)。请注意存在用于处理片段和顶点着色的独立处理器组。改编自 [Fabien Sanglard 的博客](https://fabiensanglard.net/cuda/)。
```

### 示例 2：简单图片

```markdown
![CUDA 架构图](https://files.mdnice.com/user/59/be06d295-4bee-4c05-95b4-e4b9d119af99.png)
```

## 🔧 技术实现

图片渲染由 `generate_website.py` 自动处理：

1. **识别图片**: 匹配 `![](https://...)` 格式
2. **提取说明**: 识别紧跟的 `> 引用块`
3. **生成 HTML**: 创建 `.image-container` 容器
4. **应用样式**: 自动应用 CSS 样式

## 🎯 图床推荐

推荐使用以下图床服务：

1. **MDnice 图床**: https://mdnice.com/
   - 稳定可靠
   - 速度快
   - 支持外链

2. **GitHub**: 使用 GitHub Issues 作为图床
   - 完全免费
   - CDN 加速
   - 永久有效

3. **其他图床**
   - 图壳: https://imgkr.com/
   - 路过图床: https://imgse.com/

## ⚙️ 自定义样式

如需修改图片样式，编辑 `generate_website.py` 中的 CSS：

```css
.image-container {
    margin: 30px 0;
    text-align: center;
    background: #161b22;
    border: 1px solid #30363d;
    border-radius: 8px;
    padding: 20px;
}

.image-container img {
    max-width: 100%;
    height: auto;
    border-radius: 4px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
}

.image-caption {
    margin-top: 15px;
    color: #8b949e;
    font-size: 0.9em;
    font-style: italic;
    line-height: 1.5;
}
```

## 🐛 常见问题

### Q: 图片不显示怎么办？

A: 检查以下几点：
1. 图片链接是否有效（https://...）
2. 图床是否支持外链
3. 浏览器是否阻止了图片加载
4. 检查网络连接

### Q: 图片说明不显示？

A: 确保图片和说明之间有空行：
```markdown
![](image-url)

> 说明文字  <!-- 这里必须有空行 -->
```

### Q: 如何批量替换图片链接？

A: 使用编辑器的查找替换功能，或编写脚本：
```python
import re

def replace_image_url(md_content, old_url, new_url):
    return md_content.replace(old_url, new_url)
```

## 📚 相关文档

- [README.md](README.md) - 项目说明
- [generate_website.py](generate_website.py) - 网站生成脚本
- [CHANGELOG.md](CHANGELOG.md) - 更新日志
